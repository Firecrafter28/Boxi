// Stretch
let stretchB = 35; // Full Stretch
let stretchS = 25; // Minimum Stretch
let defaultStretch = 30; // Default Stretch

// Reset Stretch
function resetstretch() {
    player.style.height = defaultStretch + 'px'; // Remove Height Stretch
    player.style.width = defaultStretch + 'px'; // Remove Width Stretch
}