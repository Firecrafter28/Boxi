function Start() {
    update(); // Update
    goalupdate(); // Update Goal
    DarkMode(); // Set To Dark Mode
    ChangeColor(); // Change Color
    timer(); // Timer
    loading.style.visibility = 'hidden'; // Hide Loading Screen
}